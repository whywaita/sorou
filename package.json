{
  "name": "sorou",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "for f in ./drizzle/migrations/*.sql; do wrangler d1 execute sorou-db --local --file=\"$f\"; done",
    "db:migrate:prod": "for f in ./drizzle/migrations/*.sql; do wrangler d1 execute sorou-db --remote --file=\"$f\"; done"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.0",
    "drizzle-orm": "^0.42.0",
    "hono": "^4.7.0",
    "workers-og": "^0.0.27",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250515.0",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.0",
    "eslint": "^9.0.0",
    "prettier": "^3.5.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.60.0",
    "vitest": "^3.1.0",
    "wrangler": "^4.21.0"
  }
}
